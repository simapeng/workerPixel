{
  "name": "email-pixel-tracker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for email pixel tracking",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply subscriptions-local --local",
    "db:migrate:prod": "wrangler d1 migrations apply subscriptions-local",
    "test": "node test.js"
  },
  "keywords": [
    "cloudflare",
    "worker",
    "email",
    "tracking",
    "pixel"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
} 